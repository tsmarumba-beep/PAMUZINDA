'use strict';

// Design tokens
const colors = {
  emeraldGreen: '#50C878', // accent color
};

const borderRadius = '8px'; // border radius

// Configuration constants
const universityProximityData = {
  uriUniversity: { distance: '2km', location: 'Street Name 1, City' },
  exampleUniversity: { distance: '5km', location: 'Street Name 2, City' },
};

module.exports = {
  colors,
  borderRadius,
  universityProximityData,
};